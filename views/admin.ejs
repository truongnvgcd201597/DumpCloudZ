<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="refresh" content="30">
    <title>Admin </title>
    <style>
        * {
            font-family: "Open Sans", sans-serif;
      padding: 0;
  margin: 0;
  border: 0;
}


body {
    width: 100%;
    height: 100vh;
  font-family: "Open Sans", sans-serif;
  font-size: 16px;
  line-height: 1.5;
  color: #333;
  background: url("../images/login-poster1.jpg");
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
    }
    .home_header{
        width: 85%;
    height: 15vh;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 5%;
      }
      .button_header{
    background: #efeffa00;
    border: 1px solid #efeffa00;
    border-radius: 5px;
    color: #efeffa;
    cursor: pointer;
    font-size: 16px;
    font-weight: 600;
    padding: 10px 20px;
    border-radius: 10px;
      }
      .button_header:hover{
        transition: 0.7s;
        color: black;
        background: #efeffa;
      }
      .logotitle{
        color: #fff;
    font-size: 42px;
    font-weight: 600;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    background: #000;
    cursor: pointer;
    font-size: 16px;
    font-weight: 600;
    padding: 10px 20px;
    border-radius: 10px;
      }
    .search_btn{
        background: #efeffa00;
        color: #fff;
    cursor: pointer;
    font-size: 16px;
    font-weight: 600;
    padding: 10px 20px;
    border-radius: 10px; 
    margin-left: 10px;
    }

    .search_btn:hover{
        transition: 0.7s;
        color: black;
        background: #efeffa;
      }
    .select{
        position: relative;
    display: flex;
    width: 200px;
    height: 38px;
    border-radius: 8px;
    overflow: hidden;
    background: #fff;       
    }   
    .select::after {
    content: '\25BC';
    position: absolute;
    top: -7px;
    right: 0;
    padding: 1em;
    background-color: #000;
    color: #fff;
    transition: .25s all ease;
    pointer-events: none;
}
    select{
        appearance: none;
    outline: 0;
    border: 0;
    box-shadow: none;
    flex: 1;
    padding: 0 1em;
    color: #000;
    background-color: var(--darkgray);
    background-image: none;
    cursor: pointer;
    }
    .search_form{
        display: flex;
    }
    .table_form {
    margin: 0 auto;
}
    </style>
</head>
<body>
    <div class="home_header">
        <h2 class="logotitle">ATN SHOP</h2>
        <div class="selectBox">
            <%- selectBox %>
        </select>
      </div>   
      <a href="/logout"><input type="button" value="logout" class="button_header"></a>
     </div>
     <div class="table_dashboard"> <%- result %> </div>
</body>
</html>